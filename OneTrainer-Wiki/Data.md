* `宽高比分桶`：使同时在不同宽高比的图像上训练模型成为可能。所有图像都被调整大小以大致匹配相同的总像素数，并根据广义宽高比进行分桶。

* `潜变量缓存`：通过保存中间数据来加速训练。如果启用此设置，一些数据将根据您的训练图像计算并保存到磁盘。

* `训练前清除缓存`：在开始训练之前清除缓存目录。仅当您想继续使用相同的缓存数据时才禁用此功能。禁用此功能可能会导致错误，如果在重新启动期间更改了其他设置。

[返回选项卡](https://github.com/Nerogar/OneTrainer/wiki/The-Program)
